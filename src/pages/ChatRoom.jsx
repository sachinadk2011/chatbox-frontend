import React from 'react';
import ChatLayout from '../component/ChatLayout';

/**
 * Home route (/)
 * All layout logic lives in the shared ChatLayout component.
 */
const ChatRoom = () => <ChatLayout />;

export default ChatRoom;